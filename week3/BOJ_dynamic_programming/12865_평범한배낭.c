#include <stdio.h>

int	n, k;
int	weight[100];
int	value[100];

int	main(void)
{
	int	i;

	// 입력 받기
	scanf("%d %d", &n, &k);
	for (i = 0 ; i < n ; i++)
		scanf("%d %d", &weight[i], &value[i]);
	
	// 초기값


	//
}
